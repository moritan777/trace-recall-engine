# Terminal Aggregation Schema Compatibility Note

The terminal aggregation validator accepts Research Logger schema v2 observations where selected thread groups and candidates may appear as either structured objects or strings. This is an offline validator compatibility fix only; Production recall behavior is unchanged.
