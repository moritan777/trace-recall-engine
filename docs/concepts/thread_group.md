# Concept: ThreadGroup

A ThreadGroup compresses multiple threads with the same canonical key before they enter Working Memory.

Internally, traces remain separate.

In the prompt, repeated traces are represented once with an occurrence count.

This prevents prompt duplication while preserving count-based recall strength.
